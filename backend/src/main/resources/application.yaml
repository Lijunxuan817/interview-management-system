spring:
  application:
    name: backend

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/ta_recruitment?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 060817
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 10000

mybatis:
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080

app:
  jwt:
    # 仅本地开发用；正式环境请改成更长的随机密钥
    secret: ta-recruitment-dev-secret-key-please-change-32bytes
    expire-hours: 24
  upload:
    dir: uploads
